# 更新日志

记录面向使用者的版本变化。日常提交汇总到待发布版本，正式发布时填写实际日期。
S0–S11 是内部开发里程碑，与产品版本号分开记录。

## 未发布

后续尚未发布的改动在此汇总。

## 2.0.0b1 — 2026-09-15

Git 标签 `v2.0.0-beta.1`，是 2.0 的首个测试版。
本节汇总旧标签 `v1.0.0`（`1553dc7`）之后的改动。测试版仍可能调整接口和行为。

### 新增

- 常驻 Core 与多客户端架构，提供 `mini core start/stop/status`、多轮聊天及统一 trace。
- Textual TUI、Web 界面与 macOS 桌面窗口，支持工具展示、审批、停止和文件/图片附件。
- 按工作区分组和恢复会话、常驻默认工作区、会话笔记、分层上下文及上下文压缩。
- 文件、Shell 和任务工具，同轮工具并行执行，以及按次和持久化权限管理。
- 子代理角色与编排、Skills、stdio/TCP MCP 扩展；桌面提供 MCP 管理和应用运行期间的计划任务。
- DDGS 搜索、Trafilatura 网页正文提取和按任务隔离的 Playwright 浏览器；
  官方 DeepSeek Anthropic 端点使用原生服务端搜索。详见 [联网能力](docs/NETWORKING.md)。

### 修复

- 会话历史持久化、不同运行的事件隔离、工作区草稿保留与 Core 连接恢复。
- 避免有副作用工具在错误后被危险地重复执行，限制文件路径越界。
- 保留模型响应块和搜索结果顺序，处理不完整流，修复 DeepSeek 上下文压缩后的请求拒绝。
- 修复服务端搜索重复执行及权限切换问题，将缓存输入计入上下文占比。
- 改善 MCP/IPC 连接稳定性、中文输入法发送行为与桌面窗口适配。

### 从旧 v1.0.0 升级

- `mini run --goal "..."` 现在连接常驻 Core。先运行 `uv run mini core start`，
  再运行任务；升级代码后重启旧 Core，确保客户端与服务端使用同一版本。
- 任务失败时 CLI 返回非零退出码；依赖旧退出行为的脚本需要调整。
- `tool.call_failed` 的错误分类字段由 `error_type` 改为 `error_class`，
  自定义事件消费者按 [WIRE_PROTOCOL.md](WIRE_PROTOCOL.md) 更新。
- 文件工具使用工作区相对路径，拒绝绝对路径和指向工作区外的符号链接。
- Python 仍要求 3.12。运行 `uv sync` 更新依赖；桌面用户运行 `uv sync --extra desktop`。
  只有浏览器工具额外需要 Node.js 20+、`npx` 和 Chrome。

### 范围与限制

- macOS `.app` 仍是依赖当前 checkout 和 `.venv` 的本机启动器，尚非独立安装包。
- 计划任务仅在应用打开期间运行；公共搜索后端可能限流或暂时无结果。
- 新增的[压缩后约束恢复实验](experiments/constraint_memory/README.md)独立于产品运行时，
  不代表已接入 Memory v1。S9–S11 仍按 [路线图](ROADMAP.md) 推进。

### 本次发布验证

- 697 项 Python 单元测试、42 项前端单元测试、3 项 Core ping/协议集成测试通过。
- mypy 检查 106 个源文件通过，生成的协议文档与代码一致。
- 源码包与 wheel 构建通过，核对包元数据、CLI/Core 版本与内置静态资源。
- 全库 Ruff 检查仍有 12 项既有问题：`config.py` 10 项行长问题、
  `skills/loader.py` 1 项行长问题、`test_memory_loader.py` 1 项未使用导入。
  因此 `make verify-s0` 尚不能全绿；本次分别执行了其相关检查。
- 本次版本整理未重跑完整桌面交互和真实模型联网验收；此前记录见对应功能文档。

## 历史标签

以下为已有 Git 标签的记录，未补造过去的 GitHub Release 或发布日期：

| 标签 | 提交 | 说明 |
| --- | --- | --- |
| `v1.0.0`、`s1.0.0` | `1553dc7` | S1：单进程 Agent 最小闭环；当时包内版本仍为 `0.0.1` |
| `s0.0` | `674aa05` | S0：项目骨架与协议契约 |

保留历史标签及其原有指向；后续发布从 2.0 测试版建立统一的版本记录。
